# CLAUDE.md — read this first, every session

<!-- Installed at bootstrap from .context_ledger/core/templates/CLAUDE.md.
ROUTER ONLY: Claude Code auto-loads THIS file, not AGENTS.md, so its only
job is landing you at the real front door. Keep it short — update
kickoff.md and the vendored core, not this pointer (core 2.0.0 trimmed
this file back down to a pointer; see CHANGELOG for why). -->

This repo runs the `.context_ledger/` engineering protocol. Before any
edit, even a one-liner: read
[`.context_ledger/kickoff.md`](.context_ledger/kickoff.md) and follow it,
in order — do not start work from memory of this file alone.

Never write under `.context_ledger/core/`.

# Environments (update in place)

Machines and sandboxes agents have run on, and what it takes to work on
this project from each. One block per environment; update the matching
block (and its "last verified" date) every time you run on it again.

## Rules

1. **Match before you add.** At session start, check whether the machine
   you're on already has a block (use its "Identify by" line). Update the
   match; add a new block only for a genuinely new environment.
2. **Record what you verified, not what you assume.** A command belongs
   under "Verified commands" only after it ran successfully on this
   environment, this project.
3. **Agents never delete blocks.** An environment the project no longer
   uses may be pruned by the user; if you can't verify a block, leave it
   alone — its last-verified date already says how stale it is.
4. **Machine facts only.** Secret values go in `secrets/`; user
   preferences in `user/`; project-wide decisions in `plans/`.

<!-- TEMPLATE — one block per environment:
---
## <stable label — hostname, "Z sandbox", "GitHub Actions ubuntu-24.04"> (last verified YYYY-MM-DD)
- **Identify by:** <how an agent recognizes this env — hostname, $USER, workspace path>
- **OS:** <e.g., macOS 15.5 / Ubuntu 24.04 sandbox>
- **Runtimes:** <node X, python Y, ...>
- **Package manager:** <npm/bun/pnpm/pip/...>
- **Verified commands:** <install / test / lint / typecheck / dev-server commands that actually worked here, with cwd if it matters>
- **Quirks:** <e.g., "no psql installed", "port 3000 usually taken", "system Python locked down">
-->

---
## Tison-Windows (last verified 2026-09-02)
- **Identify by:** Windows 11 (win32 build 26200), user `tison`, workspace `C:\Users\tison\Dev\vert`
- **OS:** Windows 11 (10.0.26200)
- **Runtimes:** node v24.13.0, npm 11.12.1, bun 1.3.13, pwsh installed (Windows PowerShell 5.1 has execution-policy-blocked scripts — use `pwsh`)
- **Package manager:** **bun** (`bun.lock` tracked; unlike Baos-Mac-mini, bun IS installed here). No `package-lock.json`.
- **Verified commands:** `bun install` (964 pkgs, prisma generate OK); `bun run lint` (eslint, ~40s); `node_modules/.bin/tsc --noEmit` (NOT `npx tsc` — that resolves to a bogus "tsc" stub package here); `bun run build` (full prisma generate + next build + standalone copy, works after 0.9.1's `scripts/standalone-copy.mjs` replaced POSIX-only `cp -r`); `npx next dev -p <free-port>` (run directly — port 3000 is held by an unrelated node.exe, don't kill it, and don't append args to the dev script); `sh .context_ledger/core/bin/ledger-gates run pre-commit` (works in Git Bash; the `.cmd` launchers are the no-shell path on Windows — paths updated for the 1.1.3 layout, re-verify on this machine next session); remote Prisma Postgres (db.prisma.io, via `.env.local`) WORKS — DB-backed flows are testable on this machine.
- **Quirks:** (1) `context-sync verify` FALSE-FAILED here (recorded 2026-09-02, core 0.8.0): git `core.autocrlf=true` checked `.context/core/` out as CRLF while MANIFEST.sha256 hashed LF → every file "FAILED". Do NOT rollback (re-checkout reproduced CRLF); verify by hashing git blobs (`git show HEAD:.context/core/<path> | sha256sum`). NOTE 2026-09-15: the project migrated to core 1.1.3 with a `.context_ledger/.gitattributes` (`eol=lf`) and LF-renormalized blobs — expected to resolve this, but **re-verify `ledger-sync verify` on THIS machine** next session. (2) `.env.local` is a Vercel CLI pull that contained `NEXTAUTH_URL=""` / `NEXTAUTH_SECRET=""` as empty strings — that breaks `next build` prerender with `ERR_INVALID_URL` in next-auth module init (vars unset = fine, empty string = broken); fixed locally with real values 2026-09-02. Don't `cat` this file — it holds real DB credentials; grep keys only. (3) `gh` CLI not installed; GitHub API rate-limited from this IP without auth.
## DESKTOP-3LRR8MD (last verified 2026-09-15)
- **Identify by:** Windows 11 (win32 build 26200), hostname `DESKTOP-3LRR8MD`, user `Lameck`, workspace `C:\Users\Lameck\Tisone\vert` — a DIFFERENT Windows machine from `Tison-Windows` (same OS build; do not merge the two entries)
- **OS:** Windows 11 (10.0.26200); shell: Git Bash
- **Package manager:** bun 1.4.2 installed (`bun run lint`/`bun run build` work); node present (v24.20.0 via npx — Vercel CLI ran on it)
- **Verified commands:** `bun run lint` (via pre-commit gate, 3 warnings = baseline); `sh .context_ledger/core/bin/ledger-gates checkpoint|run pre-commit|run exit` (Git Bash); `sh .context_ledger/core/bin/ledger-sync update --major / migrate --major / rename` — ran the full 0.8.0→1.1.3 core migration 2026-09-15; `git credential fill` (GCM, non-interactive with `GCM_INTERACTIVE=never`) yields a `repo`-scope OAuth token that works for GitHub REST (used for branch protection + API probes; `gh` CLI NOT installed); `bun install --frozen-lockfile --ignore-scripts` (sync check without tripping the Prisma DLL lock); `bunx next build` + `node scripts/standalone-copy.mjs` (full build steps, dev server running); `bun add <pkg> --ignore-scripts` (updates package.json+bun.lock safely while the dev server holds `query_engine-windows.dll.node`); `npx -y vercel login` + `npx -y vercel inspect <deploy-url> --logs` + `npx -y vercel deploy --yes` (device-flow auth with the owner clicking the printed URL; CLI NOT installed globally — npx only).
- **Quirks:** (1) `ledger-sync verify` is GREEN here despite `core.autocrlf=true`: the migrated core ships `.context_ledger/.gitattributes` (`eol=lf`) and memory blobs were renormalized to LF on migration commits — the 0.8.x CRLF false-fail does NOT reproduce on this machine. (2) Repo has NO `package-lock.json`; bun lockfile tracked. (3) Vercel auto-redeploys on push to `main`; `main` branch-protected since 2026-09-15 (force-push/deletion blocked, admin enforcement — direct pushes still fine). (4) Vercel CLI credentials persist in the Windows user profile after a device-flow login (verified 2026-09-15); a later agent can read deploy logs until the owner revokes. (5) The user's `next dev` server usually runs on :3000 here — never run `prisma generate` or kill the server; use `--ignore-scripts` installs and direct build steps.
---
## Baos-Mac-mini (last verified 2026-08-12)
- **Identify by:** hostname `Baos-Mac-mini.local`, `$USER=bao`, workspace `/Users/bao/Code/vert`
- **OS:** macOS 15.7.7 (Darwin 24.6.0, build 24G720)
- **Runtimes:** node v24.17.0, npm 11.13.0
- **Package manager:** npm (`package-lock.json` present). NOTE: repo also ships a `bun.lock` and several `package.json` scripts call `bun` (`start`, `db:seed`), but **bun is not installed on this machine** — use npm for install and the `next dev` dev server; bun-only scripts won't run here.
- **Verified commands:** `npm run dev` (Next.js dev server on :3000, tees to dev.log); `npx eslint .` (lint); `npx tsc --noEmit` (typecheck); `npx eslint src/components/vert/VertApp.tsx src/components/vert/Header.tsx src/components/vert/Sidebar.tsx src/components/vert/VideoPlayer.tsx src/components/vert/HomeFeed.tsx` (targeted lint, passed for Sessions 18–20); `npx eslint src/components/vert/VideoDetail.tsx src/components/vert/VideoPlayer.tsx src/components/vert/RelatedVideos.tsx` (targeted lint, passed for Sessions 22–23); `npx next build` (production build, passed for Sessions 18–23); no test runner or test files currently exist. `node_modules/` already installed. `sh .context_ledger/core/bin/ledger-sync verify|status|update` (protocol sync; paths updated 2026-09-15 with the core 1.1.3 migration — re-verify commands next time this machine is used).
- **Quirks:** no bun installed; no psql installed (Prisma dev DB used for local development, connection uses `pgbouncer=true`). Dev server writes `dev.log`; `start`/`build` scripts assume a bun standalone runtime not available here. **ESLint baseline is now 0 errors** (react-query migration completed 2026-07-22, Session 6) — the CI lint step is blocking, so keep it that way: new code must be lint-clean. IMPORTANT: this machine's background-shell capture truncated a full `eslint .` run once this session and reported a false "clean" — always confirm the printed problem summary line, don't trust exit-only captures. During Sessions 18–20, full `npx eslint .` also timed out in the tool environment; targeted lint for every changed product file passed and the production build completed. Browser verification in Sessions 22–25 was blocked: Sessions 22–23 had an unrelated LocalMind listener on port 3000, Session 24's tool sandbox terminated detached/background dev servers when the launching command exited, and Session 25 had no port-3000 listener. Session 26 confirmed the hosted deployment loads and renders the public watch surfaces without critical console errors, but the browser runner's geometry/scroll pass repeatedly failed on malformed empty click/evaluate payloads. Use a tool-managed persistent preview or deployed URL for watch-page browser passes; do not append arguments to the `npm run dev` script because its `tee` pipeline forwards them as shell commands. LOCAL DB (verified working 2026-07-14, after user fixed `~/.npm` ownership): the app's DATABASE_URL points at the **named** Prisma dev server `vert` on port 51214 — start it with `npx prisma dev start vert --detach` (verified). A bare `npx prisma dev` creates/starts the `default` server on DIFFERENT ports (51218+) that the app can't reach — don't use it. Stop with `npx prisma dev stop vert`. NextAuth credentials login POSTs to `/api/auth/callback/credentials` with fields `identifier` + `password` (NOT `email` — sending `email` yields a silent 401). Seed demo accounts (user1@vert.com / password123 etc.) ARE present in the local dev DB (login verified 2026-07-14). PORT 3000 (2026-08-04): was occupied by an UNRELATED app ("LocalMind", `<title>` check) — the `next dev -p 3000` script and `.claude/launch.json` both hardcode 3000. `.claude/launch.json` is **gitignored** (safe to edit); set it to `"runtimeArgs": ["next","dev"]` + `"autoPort": true` so the preview picks a free port (ran on 63588). NEXTAUTH_URL assumes :3000, so authenticated flows may misbehave off-port, but public/unauth endpoints (video detail, related, trending, tags) work fine for verification. BROWSER-PANE CLICKS: coordinate/ref `left_click` is unreliable here (confirmed again 2026-08-04, first seen Session 6) — it often doesn't register the React onClick; use programmatic `element.click()` via `javascript_tool` for navigation. Real CDP `hover` DOES work for triggering React `onMouseEnter` (dispatched synthetic `mouseover` events do NOT). Prefetch feature (Session 8) verified live this way. Also (Session 9): the browser-pane `read_network_requests` tool returns "No network requests recorded" EVEN WHEN requests are firing — don't trust it for evidence; use `preview_logs` (server request log) or DOM state instead. SEED DATA has no media: seed videos have `thumbnailUrl: null` (and channel `avatarUrl` null), so cards render placeholders and NO `<img>` renders locally — to verify an image/thumbnail feature, patch `window.fetch` in the page to inject a same-origin image URL (e.g. `/logo.svg?i=N` — `public/logo.svg` exists and decodes 150×150; `/favicon.ico` is NOT a decodable image here) then remount the feed, or test on a deploy with real thumbnails. IMAGE-OPTIMIZATION verification (Session 11, works well): hit the dev server's `/_next/image?url=<encoded real blob URL>&w=640&q=75` endpoint with `curl -H "Accept: image/avif,image/webp"` — it returns the optimized bytes + `content-type`, so you can prove AVIF/WebP conversion + size reduction against REAL media locally (measured a real 445KB blob PNG → ~29KB AVIF). For render proof, inject a real blob thumbnail via the `window.fetch` patch, remount, and check the rendered `<img>` src is rewritten to `/_next/image?url=…` and `naturalWidth>0`. BROWSER PANE (Session 33, 2026-08-11): the in-app **Claude Browser pane** (`mcp__Claude_Browser__preview_start {url}` → `navigate`/`computer`/`read_page`/`javascript_tool`/`get_page_text`/`resize_window`) works well for RESEARCH against the LIVE deploy (`https://vert-wine.vercel.app`) — no local dev server needed. Coordinate clicks mostly registered against the deployed site (better than the chrome-devtools runner used in Sessions 24–26), but a `computer` action occasionally timed out with "Browser pane is currently hidden" — take a screenshot / retry to recover. For evidence prefer JS-driven inspection: `fetch(location.href)` to read the server HTML + `<head>` metadata (confirmed watch pages have NO per-video `og:*`/title — title stays "Vert"), `document.querySelector('video')` for player state, `resize_window {preset:'mobile'|'desktop', colorScheme}` for responsive/dark checks (dark mode is class-based on `<html>`; a hidden pane may not repaint the screenshot even when the DOM class flipped — read computed styles to confirm).
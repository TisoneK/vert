# Current Task (overwrite each session)

Holds exactly one task — the one being worked on right now. Set it at
session start (Step 3), clear it at session end (Step 15). If a prior
session died mid-task, check its session entry and backlog before starting.

- **Status:** idle — no active task

_(Session 44, 2026-08-12: watch-page playback shipped as 0.9.0 — autoplay,
auto-advance/loop-if-last, persistent volume (usePlayerPrefs), poster-first;
ADR-31; live-verified. See reviews/2026-08-12-playback-review.md.)_
